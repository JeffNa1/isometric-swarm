shader_type canvas_item;

uniform vec4 vignette_color : source_color = vec4(0.9, 0.05, 0.08, 0.85);
uniform float intensity : hint_range(0.0, 1.0) = 0.0;
uniform float inner_radius : hint_range(0.0, 1.5) = 0.48;
uniform float outer_radius : hint_range(0.0, 1.8) = 1.05;

void fragment() {
	vec2 uv = UV - vec2(0.5);
	uv.x *= 1.7778;
	float dist = length(uv);
	float vignette = smoothstep(inner_radius, outer_radius, dist);
	COLOR = vec4(vignette_color.rgb, vignette * intensity * vignette_color.a);
}
